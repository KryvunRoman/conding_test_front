import React, {Component} from 'react';

import './EmptyTransactionSidebar.scss';

const EmptyTransactionSidebar = () => {

    return (
        <div className="empty__sidebar">
            Click on one or several
            <br />
            transactions to see details
        </div>
    );
};

export default EmptyTransactionSidebar;